// Flat config generated and wired up by @nuxt/eslint — https://eslint.nuxt.com
import withNuxt from './.nuxt/eslint.config.mjs'

export default withNuxt({
  rules: {
    // Header / Footer / Sidebar are intentionally single-word app components.
    'vue/multi-word-component-names': 'off',
  },
})
