{
  "name": "bhamdesigns",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0",
    "npm": ">=11"
  },
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "postgenerate": "node scripts/write-404.mjs",
    "images:build": "npm install --no-save --no-package-lock sharp && node scripts/build-gallery-images.mjs",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "typecheck": "nuxt typecheck"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  },
  "dependencies": {
    "vue": "^3.5.39",
    "vue-router": "^5.1.0"
  },
  "devDependencies": {
    "@nuxt/eslint": "^1.16.0",
    "@nuxt/fonts": "^0.14.0",
    "@nuxtjs/sitemap": "^8.2.2",
    "eslint": "^10.6.0",
    "nuxt": "^4.4.8",
    "sass": "^1.101.0",
    "typescript": "^5.9.3",
    "vue-tsc": "^3.3.7"
  }
}
